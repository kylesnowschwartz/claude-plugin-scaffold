---
description: Example command for <%= plugin_name %>
---

# Example Command

This is an example slash command. Users invoke it with `/<%= plugin_name %>:example`.

Arguments passed by the user: $ARGUMENTS

TODO: Replace this with your command implementation.
